<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:screenOrientation">portrait</item>
        <!-- 允许使用transitions -->

    </style>

    <style name="toolBar3" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">#FFFFFF</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <item name="actionMenuTextAppearance">@style/ToolbarMenuTextSize</item>

    </style>

    <style name="ToolbarMenuTextSize" parent="@style/TextAppearance.AppCompat.Menu">
        <item name="android:textSize">@dimen/ui_weight_3_size</item>
    </style>

    <!--带状态的framlayout属性-->
    <declare-styleable name="MyStatusFramlayout">
        <!--错误页面-->
        <attr name="error_layout" format="reference" />
        <!--加载中页面-->
        <attr name="loading_layout" format="reference" />
        <!--内容页面-->
        <attr name="content_layout" format="reference" />
        <!--空布局页面-->
        <attr name="empty_layout" format="reference" />
        <!--加载状态-->
        <attr name="load_status" format="enum">
            <enum name="loading" value="0" />
            <enum name="empty" value="1" />
            <enum name="error" value="2" />
        </attr>

    </declare-styleable>
</resources>